import React from 'react';

const ValidationComponent = (props) => {
    return (
        <div>
            Current text length is: {props.textLength}
        </div>
    );
};

export default ValidationComponent;